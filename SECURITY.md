# Security Policy

   This is a small front-end-only learning project with no backend or database, so the security surface is minimal.

   If you spot something that seems unsafe (e.g. a link that misuses user data, or a script issue), please open a GitHub Issue describing it, and it will be looked into.
